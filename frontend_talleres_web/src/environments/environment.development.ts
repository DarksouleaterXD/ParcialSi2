export const environment = {
  production: false,
  apiUrl: 'http://localhost:8000/api',
  /** Cambiá a true para probar solo UI sin FastAPI (token mock no sirve para /admin/users). */
  authMock: false,
};
